"""Accuracy-preserving speed benchmark for the strivee-btwb LLM stages.

These are NOT unit tests (they require a running Ollama and the local saved
captures); they are excluded from pytest collection by not matching ``test_*``.

Usage:
    python -m tests.benchmark.run_baseline    # snapshot current qwen3:8b output + timing
    python -m tests.benchmark.compare         # re-run, diff accuracy + timing vs baseline

The baseline is generated by re-running the CURRENT analyser/formatter over every
text-era capture week — it does NOT trust the committed ``parsed/`` cache, which
may have been produced by an older prompt version.
"""
